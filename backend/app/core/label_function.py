def label_chord_in_world(chord: str, world: str) -> List[str]:
    # Example label function: placeholder
    if chord in ["C", "G", "Am", "F"] and world == "C":
        return ["tonic", "dominant"]
    return []
